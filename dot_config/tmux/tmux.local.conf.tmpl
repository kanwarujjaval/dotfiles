# OS-specific tmux overrides (generated by chezmoi)
set -g default-shell {{ lookPath "fish" }}

{{ if eq .chezmoi.os "darwin" -}}
# macOS: use C-a as prefix (C-b stays for nested server sessions)
unbind C-b
set -g prefix C-a
bind C-a send-prefix
{{ end -}}
